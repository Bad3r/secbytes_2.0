{
  "license": "BSD-3-Clause",
  "scripts": {
    "build": "gatsby build && cp -R _redirects public/",
    "develop": "rm -rf .cache && gatsby develop",
    "preDeploy": "gatsby build --prefix-paths",
    "deploy": "yarn preDeploy && gh-pages -d public -b master",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "@narative/gatsby-theme-novela": "^0.13.3",
    "gatsby": "^2.19.16",
    "gatsby-plugin-manifest": "^2.2.41",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
